# DEBT.md — 已知債務與門檻（誠實帳本）

> 每筆債都寫「是什麼、為何先欠著、什麼時候該還」。發現新債往下加，還掉就劃線留紀錄。

| # | 債 | 為何先欠 | 還債門檻 |
|---|---|---|---|
| 1 | **文章圖片（media 表）存 D1 BLOB 而非 R2** | 單一資料庫的運維簡單值回票價（ADR-0002）；目前圖量小。**注意 2026-07-30 v2.4.0 只把 Playground 附件搬進 R2**（ADR-0013），文章圖片刻意沒動 — 它有 `/img/{id}` 的一年 immutable 快取與備份排除邏輯，是另一套路徑 | media 總量近 1 GB，或單月圖片流量明顯拖慢 D1 匯出／備份 |
| 2 | ~~**pg_messages 無保留策略**~~ ✅ v2.0.0 Phase I 還清：每日 cron 清 360 天前訊息（src/cron.ts purgeOld） | 對話是會員資產，不敢亂刪；量還小 | ~~表破 10 萬列：加「會員可刪」之外的過期歸檔（例：360 天）~~ |
| 3 | **BYOK（會員自帶上游金鑰）未做** | 拍板 v1 用共享金鑰＋配額（ADR-0003） | 有會員的正當用量超過管理員願意墊的錢 |
| 4 | ~~**告警只有站內（errlog／logs 頁）**~~ ✅ v2.0.0 Phase I 還清：每 5 分鐘 cron 掃 errlog 推 Telegram（secrets 未設＝自動停用） | 拍板 v1 不接 Telegram/Sentry；個人站可接受人工巡 | ~~出現「錯誤發生 >1 天才被看到」的實際案例~~ |
| 5 | ~~**無 eslint／prettier**~~ ✅ v2.0.0 Phase E 還清：ESLint flat config＋typescript-eslint＋Prettier＋CI lint job | tsc（@ts-check 新模組）＋測試已擋住大半；風格靠人肉一致 | ~~出現第二位貢獻者~~ |
| 6 | ~~**req_log 不做 usage_daily 聚合表**~~ ✅ v2.0.0 Phase I 還清：migration 0003＋每日 cron 結算（冪等），長期報告數據源 | 流量小，COUNT 走 (user_id,svc,ts) 索引足夠 | ~~req_log 破 50 萬列或 /logs 用量分頁明顯變慢~~ |
| 7 | **custom 渠道不加 stream_options.include_usage** | 自架/本地服務可能拒收未知欄位 | OpenAI 相容自架服務普遍支援後，改成 per-channel 開關 |
| 8 | ~~**舊 LOGS_TOKEN 仍在 git 歷史**~~ ✅ v2.0.0 Phase O 還清：2026-07-17 線上輪替新金鑰（舊值驗證 401）＋`git filter-repo --replace-text` 洗除全歷史（60 commit 數目不變、main 檔案樹雜湊不變、mirror 離線備份留存）；**此後永不再改寫歷史** | 私有 repo＋發佈時已輪替新值，風險受控 | ~~repo 公開前必須 `git filter-repo` 洗歷史~~ |
| 9 | **CSP style-src 保留 'unsafe-inline'** | 全站大量 inline style，改造工程大、收益小（ADR-0004） | 若要拿 CSP 滿分（如安全掃描需求）：抽出共用 <style> 或加 style nonce |
| 10 | ~~**配額算「次數」不算「錢」**~~ ✅ v2.0.0 Phase J 半清：model_prices 定價表＋stats／/logs 估算成本（回溯計價，報告用）；**配額執法仍算次數（維持拍板，不算還債缺口）** | 各家計價表是維護負擔；token 數已入庫，之後可回溯計價 | ~~單月上游帳單失控，或想出成本報告時~~ |
| 11 | ~~**D1 無自動備份**~~ ✅ v2.0.0 Phase I 還清：每日 cron 全庫 JSONL 進 R2（BLOB 排除、保留 14 份）；手動 export 指令仍在 ADMIN.md 當第二保險 | 個人站接受手動 `wrangler d1 export`（ADMIN.md 有指令） | ~~內容量大到「重寫一遍會心痛」：加 GitHub Actions 定期 export~~ |
| 12 | **vitest-pool-workers 鎖 wrangler ~4.44** | pool-workers 0.9 的相容組合（Phase A 實測 4.110 撞版） | pool-workers 出新版支援新 wrangler 時一起升 |
| 13 | ~~**串流貼著免費方案 10ms CPU 上限跑**（ADR-0011）~~ ✅ 2026-07-31 v2.6.0 還清：轉譯整段搬進 `PgStream` Durable Object（**免費方案的 DO 也是 30 秒 CPU**，10ms 那條只綁 Worker）。實測 626ms 從「預算的 6260%」變成「預算的 2%」，且不必升付費方案、不必犧牲任何功能 | ~~兩層優化後 5982 增量約 4.2ms，尚有餘裕~~ **當時的判斷是錯的**：4.2ms 只量到解析側（本機 bench 比線上低估約 95×），線上一趟 20 秒串流實際是 626ms | ~~升 Workers Paid~~ → 見 ADR-0015。新的天花板是 DO 的 GB-s 計費（免費 13,000 GB-s/日 ≈ 5200 趟串流），不是 CPU |
| 14 | **fastsse 快速路徑寫死 OpenAI 相容欄位名**（ADR-0011） | 實際流量 openai 69／gemini 17；anthropic 目前無渠道，走完整解析也不痛 | 新增第四種渠道 kind 時：要嘛補一條快速路徑，要嘛加進 `slowKind` 名單 |
| 15 | **按「停止」也會在背景把回覆跑完**（ADR-0012） | 伺服器端「關網頁」與「按停止」都只是 fetch 被中止，分不出來；先讓「關網頁看得到完整回覆」這個主要需求成立 | 會員反映「停止後歷史卻是完整回覆」→ 前端送一個明示的中止訊號（新端點＋ADR-0010 的三處文件同步） |
| 16 | ~~**斷線後長回覆仍會被截斷（約 25 秒生成時間為限）**（ADR-0012）~~ ✅ 2026-07-31 v2.6.0 還清**平台限制的部分**：生成已搬進 DO，`waitUntil` 那個 30 秒天花板不存在了（DO 只要有 pending I/O 就活著）。**但 `BG.budgetMs` 仍是 20 秒沒動** —— 天花板拆了不等於自動放寬，放寬要多花 GB-s、也會讓「按停止」後上游跑更久，值得單獨決定 | 這條門檻當年就寫對了：「升 Workers Paid，或把生成搬進 Durable Object（ADR-0007 已有依賴）自己管生命週期」—— 走的是後者 | 真的有人抱怨長回覆存不完整 → 直接把 `lib/pgchat.ts` 的 `BG.budgetMs` 調大，現在沒有硬上限擋著了 |
| 17 | **`request.signal` 掛著但實測不會觸發**（ADR-0012） | 成本趨近於零，Cloudflare 哪天補上就自動變快路徑（省下 `hangMs` 那 5 秒＝回覆多存幾百字） | 定期回測；**若確認永遠不會有，就直接刪掉**——但絕不能因此拿掉 `hangMs`，那會讓死鎖原封不動回來 |

## 2026-07-22 稽核後新增（v2.1.0）

這一批的共同點是：**現在都還沒真的出事**，寫下來是為了讓「什麼時候該處理」有客觀依據，
而不是等到出事才回頭找理由。

| # | 債 | 為何先欠 | 還債門檻 |
|---|---|---|---|
| 18 | ~~**`visits`／`errlog` 無保留策略**~~ ✅ `visits` 於 2026-07-29 還清（`RETAIN.visits`＝180 天，見 `cron.ts` purgeOld）；`errlog` 仍無保留期限。⚠ **這條只涵蓋「長期列數成長」**，跟單日寫入暴衝是兩件事 —— 後者見 **#35**，那才是真正會弄掛站台的那個 | `req_log` 與 `pg_messages` 都有 cron 輪替了，這兩張沒有；但目前約 123 次瀏覽／天，一年才 45k 列，離問題很遠 | `errlog` 破 50 萬列（`visits` 那半已還） |
| 19 | **`backupToR2` 把整庫累積成單一記憶體字串**（`cron.ts:119-139`） | isolate 上限 128 MB，現在的資料量連零頭都不到；改成串流寫入要拆掉整段邏輯 | 單次備份輸出破 20 MB |
| 20 | **R2 備份含明文機密** —— `BACKUP_TABLES` 對 `relay_channels`／`users`／`settings`／`vpn_channels` 都用 `cols:"*"`，所以每日 JSONL 含上游明文 API key、TG bot token、全體會員 VPN token，保留 14 份 | 金鑰本來就得明文存 D1 才能轉發（ADR-0003），備份含它不是 bug；`media.data` 有為 CPU 細心排除，機密則是**從沒被考慮過** | 有第二個人拿到 R2 讀權限。（或更早：實作「備份時遮罩機密欄位」——代價是備份不再能直接還原成可用系統） |
| 21 | **`vpn_token` 明文存**（`0001_baseline.sql:103`） | 同一張表的 `sessions.sid` 與 `api_key_hash` 都雜湊了，schema 註解還寫了理由；`vpn_token` 同樣是 bearer credential 卻是明文。查詢是精確比對，改雜湊完全可行、索引照用 | 下次動到 `users` 表的 migration 時順手改。**配上 #20，一份 R2 備份 ＝ 全體會員的 VPN 訂閱** |
| 22 | **`relay` 路徑片段未拒 `.`／`..`** | `encodeURIComponent` 不轉義 `.`，但 host 不可控、只在已授權上游內，打不到別的地方 | 出現帶路徑前綴的 `base_url` 渠道（例 `https://x.com/api/v1`），那時 `..` 就能往上跳出前綴 |
| 23 | **`site.ts` brand 內插未擋 `</script>`** | brand 只有管理員設得了，而管理員本來就能發 HTML 文章 | brand 改為非管理員可設 |
| 24 | **配額在本地驗證前消耗**（`chat.ts:67`） | **對稱取捨不是 bug**：先扣才不會被無效請求洗掉限流 | 出現「會員因無效請求燒光配額」的實際回報 |
| 25 | **`custom` 渠道的 `tokens_in/out` 永遠是 NULL** —— DEBT #7 決定不加 `stream_options.include_usage`，副作用是 **Phase J 的成本報表對 `custom` 渠道靜默失準**（#7 只記了原因，沒記這個後果） | 目前 custom 渠道流量為零 | 出現以 `custom` 為主力的流量，或成本報表被實際拿來做決策 |
| 26 | **`relay` 在 `ch.api_key` 為空時不帶授權標頭轉發**（`[[path]].ts:101`） | playground 有擋（`chat.ts:146`），relay 沒有 —— 會員收到的是上游的原始 401 而不是清楚的錯誤 | 有人真的建了空金鑰渠道並回報「錯誤訊息看不懂」 |
| 27 | **id_token 只驗 `aud` 與 `sub`，未驗 `iss`／`exp`；`email_verified` 用 `=== false`（缺該欄位會通過）**（`callback.ts:91-97`） | **取捨成立**：token 直接來自 Google token endpoint over TLS，符合 OIDC Core §3.1.3.7，不需要驗簽 | `iss === "https://accounts.google.com"` 與 `!== true` 各是一行的縱深防禦，**下次動到 `callback.ts` 時順手加** |

## 2026-07-31 v2.5.0 直通架構帶進來的（ADR-0014）—— ✅ 全數作廢

**這五筆全部隨著直通路徑一起消失（v2.6.0，ADR-0015）。** 它們不是「還清」的，是**問題本身不存在了**：
轉譯沒有被刪掉、只是搬進 Durable Object，所以伺服器一直看得到回覆內容。留著整段不刪，
是因為「當時為什麼認為這些代價可以接受」本身是決策紀錄的一部分。

- #28 anthropic／gemini 留在轉譯路徑 → **現在全部都在轉譯路徑，而那已經不是問題**
- #29 直通沒有背景續跑 → **還原了**，而且它寫的門檻「把生成搬進 Durable Object」正是這次做的事
- #30 `sendBeacon` 64KB 上限 → 前端不再負責落地，限制不存在
- #31 token 數由瀏覽器回報 → 改回伺服器從 usage 訊框自己讀
- #32 嗅探攔不到串流尾端追加的欄位（Venice 的 `cost`）→ 沒有嗅探了，會員看不到原始 chunk

以下保留原文（劃線＝已作廢）：


直通把「Worker 讀每一個 chunk」整段拿掉，CPU 從 626ms 掉到 6ms —— 代價是**伺服器看不到回覆內容**。
下面五筆都是同一個取捨的不同面向，一起看才完整。

| # | 債 | 為何先欠 | 還債門檻 |
|---|---|---|---|
| 28 | ~~**anthropic／gemini 渠道還在轉譯路徑上** —— 直通只認 OpenAI 相容形狀，這兩家的串流形狀不同，前端解不出來~~ ✅ 作廢 | gemini 的 chunk 數量本來就比 OpenAI 少一個量級（實際流量 openai 69／gemini 17），撞上限的機率低很多；anthropic 目前無渠道 | 這兩種渠道也出現無聲截斷 → 要嘛前端補上這兩種形狀的解析（然後放進白名單），要嘛升 Workers Paid |
| 29 | ~~**直通模式沒有「關掉網頁後在背景把回覆跑完」**（ADR-0012 的功能在這條路上不存在）~~ ✅ 作廢 | 換來的是「這種請求本來會被 CPU 殺掉、什麼都留不下」變成「活著跑完」；而且前端 `pagehide` 會用 `sendBeacon` 送出已收到的部分 | 會員實際回報「關了網頁回來只剩半截」→ 把生成搬進 Durable Object 自己管生命週期（同 #16 的解法） |
| 30 | ~~**`sendBeacon` 的 64KB 本體上限** —— 關網頁那一刻若已生成超過 64KB，那一發送不出去（正常串完那條路沒有這個限制）~~ ✅ 作廢 | 瀏覽器的硬限制，繞不過；64KB 中文約 2 萬字，一般對話遠不到 | 出現「長回覆關網頁後整篇不見」的實際案例 → 改成串流期間每 N 秒回報一次（多幾次 D1 寫入換保險） |
| 31 | ~~**直通模式的 token 數是「瀏覽器回報」的** —— 伺服器看不到 usage 訊框，只能信客戶端；只補得了一次、也只動得了自己的列，但數字本身可以被少報~~ ✅ 作廢 | 配額執法算的是**請求次數**不是 token（ADR-0003），少報 token 佔不到便宜；受影響的只有成本報表的精確度 | 成本報表被實際拿來做決策（跟 #10／#25 同一個門檻） |
| 32 | ~~**嗅探攔不到串流尾端才追加的欄位** —— 例：Venice 跟 usage 同一筆送出的 `cost:{usd,diem}`，會員打開開發者工具看得到~~ ✅ 作廢 | 這是直通架構本身的邊界，不是檢查的疏漏（判斷時位元組還沒到）；而且程式碼早就寫明「隱藏上游不是安全邊界，真正的保護是會員拿不到 base_url 與金鑰」 | 站長認為這條曝光不可接受 → `pg_passthrough='0'` 一鍵退回轉譯，或改成不向上游要 `stream_options.include_usage`（代價是連 token 都沒有） |

## 2026-07-31 v2.6.0 新增（ADR-0015）

把串流搬進 Durable Object 換來 3000 倍的 CPU 餘裕，但也換來兩筆新的債：
**新的資源會用完，而且是用牆鐘時間計費的**。

| # | 債 | 為何先欠 | 還債門檻 |
|---|---|---|---|
| 33 | **DO 的 GB-s 計費是新的天花板，而且沒有任何量測** —— 免費額度 13,000 GB-s/日，一顆 DO 128MB，所以一趟 20 秒串流吃 2.5 GB-s ≈ **每日 5200 趟**。CPU 不再是限制之後，**限制變成「模型講多久」而不是「講多少字」** —— 一個慢吞吞的模型比一個快模型貴 | 本站流量離 5200 趟/日還差好幾個數量級；而且 `req_log` 已經有 `dur_ms`，真的要算隨時算得出來 | 單日 `SUM(dur_ms)` 破 8 小時（≈ 額度的一半）→ 在 /logs 或每日 cron 加一個 GB-s 估算，別等被切斷才知道 |
| 34 | **Worker 與 DO 之間會部署漂移** —— 兩邊是同一次 `wrangler deploy` 沒錯，但 DO 實例會**沿用舊版程式碼直到被回收**，所以改完 `lib/pgchat.ts` 部署後，熱實例可能還在跑舊邏輯 | 這是 DO 的固有性質，不是我們的 bug；交棒失敗已經有 try/catch 退回 Worker 路徑並寫 errlog | 出現「部署後行為時好時壞」→ 交棒信封加一個版本號，DO 認不得就自己 `abort()` 讓實例重建 |

## 2026-08-06 第三輪稽核新增（[docs/REVIEW-2026-08.md](./docs/REVIEW-2026-08.md)）

這一批跟前面幾批不一樣：**#35 是 7 月第二輪橫掃「所有匿名 D1 寫入口」時漏掉的那一個**
（它抓到 `/api/csp-report`，卻漏掉跑在每個請求最前面的 `visitLog`），而 #37 是
**「程式註解對別的檔案下的斷言，沒有任何東西在檢查」**這一整類問題的第一個實例。
兩者都不是「新寫壞的東西」，是既有的東西第一次被看見。

| # | 債 | 為何先欠 | 還債門檻 |
|---|---|---|---|
| 35 | ~~🔴 **`visits` 是不計量的匿名 D1 寫入，且與所有有價值的寫入 1:1 共用預算**~~ ✅ **v2.6.1 還清**（見本表下方的修法說明）。原文保留： —— `router.ts:107` 在路由分派**之前**就寫一列，只要帶 `Accept: text/html` 就命中（**連不存在的路徑都算**，順便讓邊緣快取失效），沒有取樣也沒有限流。免費額度是 Workers 10 萬請求/日、D1 10 萬列寫入/日，**是同一個數字**，所以瀏覽紀錄能自己把額度用完，而額度爆掉時 D1 **連查詢都失敗**。更糟的是**告警講不出口**：`errlog` 是 D1 的表、`tgAlertScan` 要從 D1 讀它、cron 結果也寫回 D1 —— 告警管道依賴的正是它必須回報其故障的子系統 | **不是刻意的取捨，是漏掉的**。`cron.ts:274-278` 早就把風險寫得很精確並說「記在 DEBT」，但它沒有進來；#18 只涵蓋長期列數成長（不同時間尺度，180 天保留期已處理） | **這條沒有門檻 —— 該修。** 由便宜到貴：① 不記沒命中路由的請求 ② 比照 `csp-report` 取樣 ③ 用既有 `RateLimiter` DO 開一個 `visit` 命名空間（`RateCheckArg.svc` 本來就是為這種切分留的），替使用者資料保留多數額度 ④ `/api/health` 要能回報「D1 額度爆了」 |
| 36 | 🟠 **客戶端消毒器是黑名單，且放行 `style` 屬性** —— `playgroundpage.ts:293-306` 只擋 9 個元素加 `on*`；伺服器端 `lib/sanitize.ts` 是白名單、`style` 進不來。模型輸出可被提示注入影響，配上 `style-src 'unsafe-inline'`（#9）就能在**真正的登入中網域**蓋滿版假登入卡（無 script 執行，CSP 不會擋） | 目前沒有實際案例；聊天區是自己的內容、不是公開頁面 | **一行就能修**（屬性迴圈裡移除 `style`），沒有理由等門檻。順帶：大寫 `tagName` 比對對 SVG 命名空間不成立（`<svg><script>` 活得下來，今天無害因為 `innerHTML` 不執行 script），要嘛 `.toUpperCase()`，要嘛整支改成白名單 |
| 37 | 🟠 **半清（v2.6.1）**：`PG_LIMITS` 的三個常數已納入 `tools/check-docs.mjs`（見下方），四句錯誤敘述也已更正；**但「註解對其他檔案的斷言」這一整類仍然只能靠人**。原文保留：**程式註解對其他檔案的斷言沒有任何檢查者** —— 例：`csp-report.ts:4` 寫「全站**唯一**的匿名 D1 寫入口」（錯，見 #35）、`cron.ts` 寫「記在 DEBT」（沒有）、`modelcaps.ts:24-25` 還寫「maxImgPerMsg（4）與 maxImgBytesTotal（1.5MB）」（實際 10 與 16MB）、`API.md:515` 寫「中斷連線＝停止生成」（與 ADR-0012 的主打功能相反）。`tools/check-docs.mjs` 已經把測試數／E2E 數／版本號／對照表架構列接上 CI，**這一類落在它還沒涵蓋的範圍**：同時以常數存在於程式、以主張存在於散文的數字 | 這類漂移不會讓任何測試變紅，只會讓讀者對整份文件失去信任（`AUDIT-2026-07` 開頭講的正是這件事） | 把 `PG_LIMITS` 納入 `check-docs.mjs`（讀出常數，要求文件裡引述的 MB 數一致）→ (a) 這類自動變紅燈。散文類的只能靠改，但**改 API.md 那句是現在就該做的**，它會誤導 API 使用者 |
| 38 | ⚪ **（仍未還）`/relay` 仍然沒有 E2E** —— 2026-07 稽核誠實揭露、至今仍在。現在有 6 條 E2E，第六條（v2.3.4）是數學佔位符回歸，不是 relay | 整合層有覆蓋（`relay.test.ts`／`relay-metering.test.ts`），所以缺的是端到端信心而不是正確性證據 | 重申而不是新增：不讓「當時決定先不修」默默熟成為「沒有人記得決定過」。動到 relay 轉發路徑時補上 |

### #35 的修法（v2.6.1，2026-08-06）

兩層，**兩層都不加 I/O 到使用者的關鍵路徑上**：

- **L1 頁面白名單**（`routes/_middleware.ts`）：只記站上真的有的路徑。
  觸發條件以前是「帶了 `Accept: text/html`」，那等於**任何路徑都記、連不存在的都記**——
  掃描器打 `/wp-admin`、`/.env`、`/phpmyadmin` 每一發都寫一列，而且每個路徑都不同，
  順便讓邊緣快取失效。零成本，而且它擋掉的正是最現實的那種流量。
- **L2 全站每日上限**（`VISIT_GUARD`，40000 列＝免費寫入額度的 40%）：計數走既有的
  `RateLimiter` DO（`visit:global`），**1/20 抽樣**才問一次，判定結果在該 isolate 快取到隔天。
  正常流量（約 123 次瀏覽／天）一天約 6 次 DO 呼叫；被灌的時候一個取樣區間內就收斂，
  之後連 DO 都不再呼叫。**拒絕與例外都停止記錄**——刻意與 `lib/quota.ts` 的 fail-open 相反，
  理由同 `csp-report`：被保護的是「有使用者資料的那些寫入」的預算，所以最便宜的那筆該讓路。

**真正的重點不是上限，是上限的可觀測性**：跳掉時寫一筆 `visits.cap` 進 errlog，
既有的五分鐘 `tgAlertScan` 就會推到 Telegram —— 而**此刻 D1 還活著**（自訂上限離平台上限
還有 60% 餘裕）。這才是對 #35 核心問題的回答：**對前導指標告警，而不是對那個會讓告警管道
一起消失的故障告警。**

另外 `/api/health` 現在把 D1 錯誤分類成 `quota`／`error`／`unbound`（`db_error` 欄位）。
它回答時不需要任何 D1 **寫入**，所以額度爆掉時它仍然是全站唯一講得出話的地方——
而那兩種狀況的處置完全相反（等 UTC 午夜／升方案，vs 看平台狀態頁）。

測試：`test/int/middleware.test.ts`（白名單、抽樣換算、拒絕與例外都停、跨日重置、
停止後不再呼叫 DO、上限不影響網站回應）與 `test/int/smoke.test.ts`（D1 錯誤分類）。

### #37 的修法（v2.6.1）：一條可被檢查的慣例

`tools/check-docs.mjs` 新增「程式常數 × 散文數字」檢查，但它靠的是一條**慣例**：

> **在散文裡引述某個常數的值時，要把常數名一起寫出來。**

有常數名當錨點，就能精準只驗「這一段在講這個常數」的地方，不必去猜文件裡每個「16MB」
指的是不是它 —— 寬鬆比對必然誤判，然後被加白名單，然後整個檢查失效。
**沒寫常數名的數字驗不到，這是已知的涵蓋缺口、不是疏漏**，而它剛好把誘因指向對的方向。
`docs/adr/**` 與稽核報告**刻意排除**：那些是有日期的紀錄，本來就該保留寫作當下的數字
（ADR-0013 的 1.5MB 是對的，另外用補記說明變更）。逼人竄改紀錄比數字過時更糟。
